import React from "react";
import "./loading.scss"

export default function Loading() {
    return (
        <h2 className="loading">
            Loading<span>.</span>
            <span>.</span>
            <span>.</span>
        </h2>
    );
}
